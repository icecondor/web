div.navbar
  div.flexbox
    div.flexbox
      a.logo.inline(href="/")
        img(src="/assets/logo.svg")
      div.inline
        a.logo.inline(href="/")
          | IceCondor

      ul.inline-menu.inline#navmenu

    div.flexbox
      div.userarea.hidden
        span.username.inline
        ul.menu.hidden
          li.logout
            a Logout

      div.hamburger.hidden
        svg(width="30" height="30" id="icoOpen")
          path(d="M0,5 30,5" stroke="#fff" stroke-width="5")
          path(d="M0,14 30,14" stroke="#fff" stroke-width="5")
          path(d="M0,23 30,23" stroke="#fff" stroke-width="5")

      div.login_box.inline
        form.token_email
          input#login(type="email" placeholder="email address" name="email")
          button(type="submit") Sign in

script.
  $(function(){
    $('.token_email').submit(function(evt){
      emailTokenRequest(evt.target)
      evt.preventDefault()
    })
    $('.hamburger').click(function(evt){
      showLogin()
    })
    $('.menu .logout').click(function(evt){
      clearKey()
      window.location = "/"
    })
    if(getKey()) {
      $('.login_box').addClass('hidden')
      $('.hamburger').addClass('hidden')
      $('.userarea').removeClass('hidden')
    }
    $('.navbar .userarea').click(function(evt){
      $('.navbar .userarea .menu').removeClass('hidden')
      evt.preventDefault()
    })
  })

  function showLogin(){
    $('.hamburger').addClass('hidden')
    $('.login_box').removeClass('hidden')
    $('.navbar .flexbox').removeClass('flexbox')
  }

